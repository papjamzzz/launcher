# Launcher — Environment Variables
# Copy to .env and fill in values

# No API keys required for Launcher
# It only monitors local ports — no external services needed

# Optional: override default port
# PORT=5554
